/**
 * lwyf
 * https://github.com/LWYF/Learn
 */

'use strict';

import {Dimensions} from 'react-native';
const PixUtil = {
    leftScale: 0.7,
    navigationBar: 64,
    size: {
        width: Dimensions.get('window').width,
        height: Dimensions.get('window').height
    }
}

export default PixUtil;